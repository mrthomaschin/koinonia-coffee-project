rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow Firebase Admin SDK (backend) full access
    allow read, write: if request.auth == null;
    
    // Deny all client-side access
    allow read, write: if false;
  }
}
