rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Account sessions and cached Notion data are backend-only. Firebase Admin
    // SDK calls bypass these rules.
    allow read, write: if false;
  }
}
